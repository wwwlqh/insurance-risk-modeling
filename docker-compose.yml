version: "3.9"

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:7860"
    volumes:
      - ./models:/app/models   # hot-swap models without rebuilding
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:7860/"]
      interval: 30s
      timeout: 10s
      retries: 3

  streamlit:
    build:
      context: .
      dockerfile: Dockerfile.streamlit
    ports:
      - "8501:7860"
    environment:
      - API_URL=http://api:7860   # internal Docker network
    depends_on:
      - api
